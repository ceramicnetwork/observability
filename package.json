{
  "name": "ceramic-observability",
  "version": "1.0.3",
  "private": true,
  "packageManager": "pnpm@7.9.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "turbo run test",
    "lint": "turbo run lint",
    "build": "pnpm run -r build:types && turbo run build:js",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "@skypack/package-check": "^0.2.2",
    "@swc/cli": "^0.1.62",
    "@swc/core": "^1.3.68",
    "@swc/jest": "^0.2.26",
    "jest": "^29.6.1",
    "prettier": "latest",
    "turbo": "latest",
    "typescript": "^5.1.6"
  }
}
